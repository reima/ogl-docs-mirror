<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE book PUBLIC "-//OASIS//DTD DocBook MathML Module V1.1b1//EN"
	      "http://www.oasis-open.org/docbook/xml/mathml/1.1CR1/dbmathml.dtd">
<refentry id="ImpDepConstants">

    <refmeta>
	<refentrytitle>ImpDepConstants</refentrytitle>
	<manvolnum>3G</manvolnum>
    </refmeta>
    <refnamediv>
	<refname>ImpDepConstants</refname>
	<refpurpose>Describes Implementation-Dependent API Values for GLSL</refpurpose>
    </refnamediv>

    <refsect1><title>Description</title>
	<para>A number of new implementation-dependent values have been
	defined in OpenGL 2.0 to support the requirements of the OpenGL
	Shading Language. Each of these values can be queried by calling
	one of the variants of
	<citerefentry><refentrytitle>glGet</refentrytitle></citerefentry>.</para>

	<variablelist>
	    <varlistentry>
		<term><constant>GL_MAX_COMBINED_TEXTURE_IMAGE_UNITS</constant></term>
		<listitem>
		    <para> Defines the total number of hardware units
		    that can be used to access texture maps from the
		    vertex processor and the fragment processor
		    combined. The minimum legal value is 2.</para>
		</listitem>
	    </varlistentry>

	    <varlistentry>
		<term><constant>GL_MAX_DRAW_BUFFERS</constant></term>
		<listitem>
		    <para> Defines the maximum number of buffers that
		    can be simultaneously written into from within a
		    fragment shader using the special output variable
		    array <code>gl_FragData</code>. This
		    constant effectively defines the size of the
		    <code>gl_FragData</code> array.</para>
		</listitem>
	    </varlistentry>

	    <varlistentry>
		<term><constant>GL_MAX_FRAGMENT_UNIFORM_COMPONENTS</constant></term>
		<listitem>
		    <para> Defines the number of components (i.e.,
		    floating-point values) that are available for
		    fragment shader uniform variables. The minimum legal
		    value is 64.</para>
		</listitem>
	    </varlistentry>

	    <varlistentry>
		<term><constant>GL_MAX_TEXTURE_COORDS</constant></term>
		<listitem>
		    <para> Defines the number of texture coordinate sets
		    that are available. The minimum legal value is
		    2.</para>
		</listitem>
	    </varlistentry>

	    <varlistentry>
		<term><constant>GL_MAX_TEXTURE_IMAGE_UNITS</constant></term>
		<listitem>
		    <para> Defines the total number of hardware units
		    that can be used to access texture maps from the
		    fragment processor. The minimum legal value is
		    2.</para>
		</listitem>
	    </varlistentry>

	    <varlistentry>
		<term><constant>GL_MAX_VARYING_FLOATS</constant></term>
		<listitem>
		    <para> Defines the number of floating-point
		    variables avalable for varying variables. The
		    minimum legal value is 32.</para>
		</listitem>
	    </varlistentry>

	    <varlistentry>
		<term><constant>GL_MAX_VERTEX_ATTRIBS</constant></term>
		<listitem>
		    <para> Defines the number of active vertex
		    attributes that are available. The minimum legal
		    value is 16.</para>
		</listitem>
	    </varlistentry>

	    <varlistentry>
		<term><constant>GL_MAX_VERTEX_TEXTURE_IMAGE_UNITS</constant></term>
		<listitem>
		    <para> Defines the number of hardware units that can
		    be used to access texture maps from the vertex
		    processor. The minimum legal value is 0.</para>
		</listitem>
	    </varlistentry>

	    <varlistentry>
		<term><constant>GL_MAX_VERTEX_UNIFORM_COMPONENTS</constant></term>
		<listitem>
		    <para> Defines the number of components (i.e.,
		    floating-point values) that are available for vertex
		    shader uniform variables. The minimum legal value is
		    512.</para>
		</listitem>
	    </varlistentry>
	</variablelist>
    </refsect1>

    <refsect1><title>Other Queryable Values for GLSL</title>
	<variablelist>
	    <varlistentry>
		<term><constant>GL_CURRENT_PROGRAM</constant></term>
		<listitem>
		    <para> Contains the name of the program object that
		    is currently installed as part of current state. If
		    no program object is active, this value will be 0.
		    This value can be obtained by calling one of the
		    variants of
		    <citerefentry><refentrytitle>glGet</refentrytitle></citerefentry>.</para>
		</listitem>
	    </varlistentry>

	    <varlistentry>
		<term><constant>GL_SHADING_LANGUAGE_VERSION</constant></term>
		<listitem>
		    <para> Contains the OpenGL Shading Language version
		    that is supported by the implementation. It is
		    organized as a dot-delimited sequence of multi-digit
		    integers. This value can be obtained by calling
		    <citerefentry><refentrytitle>glGetString</refentrytitle></citerefentry></para>
		</listitem>
	    </varlistentry>
	</variablelist>
    </refsect1>
</refentry>
